/**
 * Test case for g.
 * Runs with mocha.
 */
'use strict'

const g = require('../lib/g.js')
const assert = require('assert')
const co = require('co')

describe('g', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('G', () => co(function * () {

  }))
})

/* global describe, before, after, it */
